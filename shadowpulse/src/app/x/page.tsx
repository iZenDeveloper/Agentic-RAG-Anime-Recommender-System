import { PlatformLanding } from "@/components/PlatformLanding";

export default function XLanding() {
  return (
    <PlatformLanding
      platform="x"
      title="Check X (Twitter) shadowban signals"
      blurb="MVP priority: account status, search suggestion, from:handle search, ghost/reply signals — always with confidence."
    />
  );
}
